# Eval Papers Directory

Drop 5 small academic PDFs here for the eval suite to run against.
Each PDF must have a corresponding entry in `../golden.json` keyed by filename.

The eval requires at minimum 1 PDF + 1 golden entry to run meaningfully.
